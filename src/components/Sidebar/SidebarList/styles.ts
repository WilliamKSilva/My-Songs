import styled from "styled-components";

export const Container = styled.ul`
    margin: 0;
    padding: 0;

    li {
        padding: 1rem 1rem;
        font-size: 15px;
        color: var(--text);
    }
`;